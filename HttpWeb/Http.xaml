<UserControl x:Class="Primo.MIA.Http"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Иконка активности -->
        <Image Grid.Column="0" 
               Width="32" 
               Height="32" 
               Margin="5"
               VerticalAlignment="Center"
               Source="pack://application:,,,/Primo.MIA;component/images/http.png"/>

        <!-- Текстовое описание -->
        <StackPanel Grid.Column="1" 
                    VerticalAlignment="Center" 
                    Margin="5">
            <TextBlock Text="HTTP Запрос" 
                       FontWeight="Bold" 
                       FontSize="12"/>
            <TextBlock FontSize="10" 
                       Foreground="Gray">
                <Run Text="{Binding Path=Prop_Method, Mode=OneWay}"/>
                <Run Text=" "/>
                <Run Text="{Binding Path=Prop_Url, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
