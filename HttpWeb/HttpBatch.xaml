<UserControl x:Class="Primo.MIA.HttpBatch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" Margin="0,0,0,5">

    <Grid Background="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Иконка -->
        <Image Grid.Column="0" Width="32" Height="32" Margin="5" VerticalAlignment="Center"
               Source="pack://application:,,,/Primo.MIA;component/images/http.png"/>

        <!-- Содержимое -->
        <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="5,0,5,5">
            <!-- Заголовок -->
            <TextBlock FontWeight="Bold" FontSize="12" Text="HTTP: Пакетные запросы" Foreground="#FF0E0E0E"/>

            <!-- Таблица запросов -->
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Таблица запросов:" VerticalAlignment="Center" FontSize="11" Foreground="#FF0E0E0E"/>
                <TextBox Grid.Column="1" Text="{Binding Prop_RequestsTable, UpdateSourceTrigger=PropertyChanged}"
                         Height="20" FontSize="11" Margin="2,0,0,0"/>
            </Grid>

            <!-- Параметры -->
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Макс. паралл.:" VerticalAlignment="Center" FontSize="11" Foreground="#FF0E0E0E"/>
                <TextBox Grid.Column="1" Text="{Binding Prop_MaxParallel, UpdateSourceTrigger=PropertyChanged}"
                         Height="20" FontSize="11" Margin="2,0,5,0"/>
                <TextBlock Grid.Column="2" Text="Таймаут (сек):" VerticalAlignment="Center" FontSize="11" Foreground="#FF0E0E0E"/>
                <TextBox Grid.Column="3" Text="{Binding Prop_TimeoutSeconds, UpdateSourceTrigger=PropertyChanged}"
                         Height="20" FontSize="11" Margin="2,0,0,0"/>
            </Grid>

            <!-- Чекбоксы -->
            <CheckBox Content="Остановить при ошибке" IsChecked="{Binding Prop_StopOnError}"
                      FontSize="11" Margin="0,2,0,0" Foreground="#FF0E0E0E"/>
        </StackPanel>
    </Grid>
</UserControl>
